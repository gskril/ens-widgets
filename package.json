{
  "name": "ens-widget",
  "version": "0.0.0",
  "description": "React components for ENS",
  "main": "packages/widget/src/index.ts",
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "dev": "yarn workspace widget run dev && yarn workspace example run dev",
    "dev:widget": "yarn workspace widget run dev",
    "dev:example": "yarn workspace example run dev"
  },
  "devDependencies": {
    "ethers": "^5.7.2",
    "prettier": "^2.8.2",
    "wagmi": "^0.10.10"
  }
}
